package com.pop.popcoinsystem;

public class Sync {

    //同步区块步骤
    //├── 验证创世区块一致性
    //│   └── 不一致 → 断开连接
    //│
    //├── 获取链状态信息
    //│
    //├── 对比工作量与高度
    //│   ├── 远程工作量更大
    //│   │   ├── 本地链落后 → 请求区块头（本地哈希 → 远程哈希）
    //│   │   ├── 高度相同 → 请求分叉点（创世哈希）
    //│   │   └── 本地链更高 → 请求完整链（创世哈希 → 远程哈希）
    //│   ├── 本地工作量更大 → 无需操作
    //│   └── 工作量相同
    //│       ├── 本地链落后 → 请求区块头
    //│       └── 高度相同且哈希不同 → 等待确认
    //│
    //├── 发送区块头请求
    //│
    //├── 处理区块头响应
    //│   ├── 验证区块头
    //│   │   ├── 校验前一哈希
    //│   │   ├── 验证默克尔根
    //│   │   └── 计算工作量
    //│   └── 验证失败 → 断开连接
    //│
    //├── 请求区块数据
    //│
    //├── 处理区块数据
    //│   └── 验证并存储
    //│





}
