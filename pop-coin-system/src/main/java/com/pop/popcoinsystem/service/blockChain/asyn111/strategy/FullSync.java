package com.pop.popcoinsystem.service.blockChain.asyn111.strategy;

/**
 * 全量同步（Full Sync）—— 全节点专用
 * 全节点需要存储完整的区块链数据（所有区块、交易、状态），并独立验证每笔交易和区块的合法性。
 *
 * 步骤：
 * 从网络获取 “区块头链”（先同步所有区块的头部，包含哈希、高度、时间戳、共识证明等关键信息），快速确定最长有效链的结构。
 * 按高度顺序（或并行）下载区块体（包含交易列表），并验证每个区块：
 * 检查区块哈希是否符合共识规则（如 PoW 的算力要求、PoS 的签名验证）。
 * 验证区块内所有交易的合法性（如签名、余额是否充足）。
 * 确保区块的前序哈希与上一区块的哈希一致（维护链的连续性）。
 * 同步 “状态数据”（如以太坊的账户余额、智能合约状态），通过区块中的交易执行结果更新本地状态数据库（如以太坊的 Merkle Patricia Tree）。
 *
 * 优化：
 * 并行下载：从多个节点同时下载不同范围的区块（如按高度分片），提升速度。
 * 断点续传：记录已同步的最高区块高度，中断后（如断电）重启时从该高度继续下载。
 */
public class FullSync {


}
